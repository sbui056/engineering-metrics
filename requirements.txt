pandas>=2.0,<3  # 3.x passes tests too; pinned so unattended CI can't drift majors silently
pyarrow>=14.0
requests>=2.31
networkx>=3.2
scipy>=1.11  # networkx pagerank delegates to scipy at runtime
streamlit>=1.45
rapidfuzz>=3.6
tqdm>=4.66
python-dotenv>=1.0
pyyaml>=6.0
pytest>=8.0
altair>=5.0
